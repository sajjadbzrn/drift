# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# release workflow scratch file (composed from CHANGELOG.md during CI)
release-notes.md

# TypeScript incremental build caches
*.tsbuildinfo

# update signing keys (never commit the private key)
.keys/

# AI assistant planning state
.mimocode/

# installed agent skills (local tooling, not part of the app)
.agents/
.claude/
skills-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# embedded doc site (separate git repo, not part of drift)
doc/

.tauri-env